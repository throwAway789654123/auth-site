{
  "name": "storagestate-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Banc d'essai pour storageState Playwright avec authentification multi-roles",
  "scripts": {
    "serve": "npx --yes serve ./site -l 4173",
    "test": "playwright test",
    "ui": "playwright test --ui",
    "test:admin": "playwright test --project=setup --project=admin",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
